{
  "name": "daily-stanza-firestore-tests",
  "private": true,
  "engines": {
    "node": "22"
  },
  "scripts": {
    "test": "jest --runInBand --testMatch='**/test/**/*.test.js'"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.2",
    "firebase-tools": "^13.0.0",
    "jest": "^29.7.0"
  }
}